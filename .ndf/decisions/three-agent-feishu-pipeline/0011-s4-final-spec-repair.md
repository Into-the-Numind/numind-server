# S4 最终 Spec 复核修复

- 日期：2026-07-20
- 复核结论：Quality/Security PASS（P0/P1/P2=0）；Spec 首次最终复核仍 FAIL（P0=0/P1=2/P2=1）。三项均作为契约错误修复，不降级为测试细节。
- Agent 1 原始字段血缘：`xhs_note_list(full)` fixture 现在提供工具正式 schema 的全部采集字段。每个 Agent 1 runner 场景执行前，血缘校验器逐步建立 full source map，并将每次 Base create/upsert 的 12 个原始字段逐值与此前 full 结果或契约缺省值核对；缺少 full source 或编造互动数等值直接失败。另有负向测试证明篡改点赞数被拒绝。
- Agent 1 目标确认：新增“本轮没有链接/确认标题”场景，必须先走真实 `ask_user_question` 且不得搜索/创建。Base 搜索 0/1/>1 的三个场景都在用户输入中明确确认精确标题“爆款素材分析库”后才执行搜索。
- Agent 3 主语自检：非人设型正式选题固定写 `-`；人设型必须填写具体主体。validator 逐选题联合校验 `归属小类` 与 `主语自检`，防止只检查字段存在却放过语义冲突。
- 额外边界：34 字段完成记录仍要求字段键存在，但 `采集时间` 遵循正式 schema，在来源缺失时允许留空；其他需要缺省文字的原始字段不得留空。
