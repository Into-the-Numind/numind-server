# 关键诊断锁定

**Date:** 2026-05-15

**Feature:** `credits-system-data-consistency`

**Migrated from:** `build-manifest.yaml` decisions[]

---

credit_package 4 个 INSERT 入口（RechargeCredits/RechargeWithOrderTx/GrantMembership/deductCreditsTxFull UpdatePackage）+ 状态机 cron 仍跑；trial_grant / user_booster_balance 不在 helper.go AutoMigrate 列表（独立 migration）；usage_record.credits_deducted 死字段证实（grep 无 reader + prod SUM=0/3274 行）。
