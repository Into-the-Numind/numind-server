# T4 完成 v2.1.26 + Schema DROP on prod

**Date:** 2026-05-18

**Feature:** `legacy-system-deprecation`

**Migrated from:** `build-manifest.yaml` decisions[]

---

T4 subagent 写 forward + rollback SQL；删 User 5 struct 字段 + BillingModeCredits 常量。Subagent 自动补全 T2/T3 遗漏：biz/customer/customer.go 整段 100 行 dead code 删、biz/user CreateCustomer 简化、controller/v1/customer UpdateSubUserTier handler + route 删、admin_dashboard SELECT user_tier 改、store/customer ResetMonthlySopRuns + GetUsersNeedMonthlyReset + UpdateSubUserTierWithLog 删，DTO admin/customer 整段缩简，2448 test PASS。prod backup 325MB at /root/backups/numind-prod-pre-t4-20260518T110049Z.sql。Dev DB 3-step dry-run（forward → rollback → forward）全通过，binary 在 schema 变更前后均 healthy。Tag v2.1.26 → prod CI deploy v2.1.26 binary → 直接执行 ALTER TABLE user DROP COLUMN (5 列) + RENAME tier_change_log TO legacy_tier_change_log on prod DB → ALL 5 LEGACY COLUMNS GONE + healthz green。
