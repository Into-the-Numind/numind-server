# 技术决策 - AI 自决

**Date:** 2026-05-19

**Feature:** `settings-credits-display-broken`

**Migrated from:** `build-manifest.yaml` decisions[]

---

后端只加注释不重命名 BoosterTotal 字段（重命名会再次破坏前端，已学到 Audit cleanup 教训）；前端用 store 的 displayState 而非 userInfo.tier 判定会员状态（credits-only 模式后 tier 字段不可靠）；user store quota* 死字段保留不删（删除属 scope creep，且永远为 0 无害）。
