# S2 spec D1-D5

**Date:** 2026-04-29

**Feature:** `membership-credits-redesign`

**Migrated from:** `build-manifest.yaml` decisions[]

---

5 个细节决策锁定——D1 subscription 加 total_months_purchased 列；D2 客户端 UUID via HTTP Idempotency-Key header（Stripe 风格）；D3 booster 父代购 API 允许（payer_id != user_id 复用 /v1/orders）但前端不暴露；D4 maintenance window 用 MAINTENANCE_MODE=true 部署版本拒写；D5 半开区间严格不留 1 秒宽容
