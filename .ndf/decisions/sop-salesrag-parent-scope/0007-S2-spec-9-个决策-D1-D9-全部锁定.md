# S2 spec 9 个决策 D1-D9 全部锁定

**Date:** 2026-05-18

**Feature:** `sop-salesrag-parent-scope`

**Migrated from:** `build-manifest.yaml` decisions[]

---

D1 SOP creator_user_id 语义升级为'始终存父账户 id'（不重命名字段，注释加强）; D2 dispatch 上移到 biz 层（顺手修 middleware→store 直调的 layer violation）; D3 sales_agent_owner 极简表（INT UNSIGNED PK，FK CASCADE，无软删 无 updated_at）; D4 索引不加复合（5 年规模单列足够）; D5 字段不重命名（GORM rename column 风险大）; D6 admin Create 路径也修（强制传 adminUserID，否则下次又制造 NULL）; D7 SQL 加 IS NOT NULL 防御; D8 biz 层 defense-in-depth assertion（user.ParentUserID==nil）; D9 chatbot 创建路径不审查（出范围）
