# 老 yaml 处理用户决策

**Date:** 2026-05-19

**Feature:** `cicd-pipeline-migration-tcr`

**Migrated from:** `build-manifest.yaml` decisions[]

---

用户拍板不删 .github/workflows/ci-cd.yml 文件，只通过 GitHub Web UI Disable workflow + yaml 顶部加 22 行 DEPRECATED 注释块（含新部署命令 + 重新启用步骤）。Deploy Key 和 GitHub Secrets 也不撤销。理由：保留切回老链路的 fallback 能力。
