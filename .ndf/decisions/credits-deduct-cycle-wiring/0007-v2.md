# v2.1.18 prod CI 失败 + 手动恢复 + ~5-8min outage

**Date:** 2026-05-15

**Feature:** `credits-deduct-cycle-wiring`

**Migrated from:** `build-manifest.yaml` decisions[]

---

打 v2.1.18 tag 携 Reconcile top-up fix。Prod CI deploy_product 阶段 Docker Hub GFW 偶发超时，但 deploy script 有 bug — docker pull 失败仍执行 docker run，把老 numind-server-prod 容器 stop 后无法启动新容器。Prod backend 中断 5-8 分钟。手动恢复：SSH prod，docker.1ms.run mirror 拉 v2.1.18 镜像，从 CI log 还原 docker run 命令重启容器。Verify: healthy + healthz 200 + 真实用户在登录使用 + DB 数据完整（37 行 credit_cycle/Wendy expires 6-14/user 30 cycle=500 包含 500 奖励）。Deploy script bug 留作未来 CI cleanup task。
