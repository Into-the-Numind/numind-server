# S5 验收 PASS

**Date:** 2026-05-14

**Feature:** `credits-deduct-cycle-wiring`

**Migrated from:** `build-manifest.yaml` decisions[]

---

L1 task test 全包 PASS (exit 0, no FAIL)。Merge develop + push, dev CI 一次 success (无需 manual mirror pull this time)。Dev container 上来 GORM AutoMigrate 自动加好 source_type / source_id 列 (DESCRIBE credit_reservation_item 确认)。L2 curl 验证 admin (cycle=2000, billing_mode=credits): /v1/credits/estimate 返回 sub_remain=2000 (从新 credit_cycle 读，非老 credit_package), trial_remain=0 (新字段), sufficient=true。这正是 bug 修复点：原本 sub_remain=0 → total=0+0+0=0 → ErrInsufficientCredits；现在 sub_remain=2000 → sufficient=true。L3 Playwright Path 1+6 跳过（测的是 grant flow，本 feature 不动 grant）。stage S5 → S6 (dev 已部署 + 验证完毕，等用户拍板 prod tag)。
