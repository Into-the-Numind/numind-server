# 飞书授权刷新恢复 — S5 验收记录

日期：2026-07-15

## 验收结论

通过。该修复只修改后端飞书授权会话和任务恢复的持久化边界；没有前端界面、数据库 schema、配置或凭据改动。

## 覆盖的验收场景

| 场景 | 证据 | 结果 |
| --- | --- | --- |
| 刷新后任务摘要绑定新会话，立即继续不报内部错误 | 生命周期回归测试 | 通过 |
| 替换会话在事务后启动并放行 | 授权服务集成测试 | 通过 |
| 新工作器不能启动时，原卡片仍可再次刷新 | 授权服务补偿测试 | 通过 |
| 第一次补偿存储暂时失败后，原卡片再次点击可自我修复 | 授权服务重试测试 | 通过 |
| 旧卡片不能停止或覆盖已获有效租约的新工作器 | 存储租约测试与并发竞态测试 | 通过 |
| 不持久化临时 URL、设备码或凭据 | 最终差异和独立审查 | 通过 |

## 自动化验证

- `go test ./internal/numind/biz/feishu -count=1`：通过。
- `go test ./internal/numind/store -count=1`：通过。
- `task test`：通过（仅有 macOS SQLite 的已知编译警告）。
- `task lint`：通过（仅有同一已知编译警告）。

## 非适用项

- 浏览器验收：本次没有用户端代码改动；待 dev 部署后以真实飞书账号做一次人工端到端验证。
- Langfuse 验收：本次没有模型调用、提示词或 Agent 推理链改动。

