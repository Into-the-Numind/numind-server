# 飞书授权刷新恢复 — 最终审查

日期：2026-07-15

## 范围

审查授权链接刷新、任务摘要重绑、启动失败补偿、补偿失败后的原卡片重试，以及旧卡片与新工作器并发竞争的边界。

## 结论

两项独立只读审查均为 **PASS**，无 P0、P1 或 P2 发现。

## 已确认的安全属性

- 替换和旧绑定恢复均按用户、代际、任务、等待状态、会话、阶段和权限范围精确围栏。
- 若替换会话存在未过期工作租约，旧卡片无法覆盖或取消它。
- 本地停止替换工作器发生在持久化恢复事务成功之后。
- 临时 URL、设备码和凭据未写入新的持久化路径。

