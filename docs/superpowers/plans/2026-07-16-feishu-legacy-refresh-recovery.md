# 飞书历史授权卡片恢复计划

## 目标

让修复上线前遗留的“任务摘要指向已作废会话”卡片安全地重新生成授权链接。

## 实施步骤

1. 保留已经提交的失败回归测试，证明历史卡片当前返回 500。
2. 在生命周期服务中，仅为任务摘要仍精确指向该 `superseded` 会话的情况调用现有操作型刷新服务。
3. 在授权服务与原子存储替换中允许该受限历史源状态；保留全部用户、代际、任务、阶段与权限范围围栏。
4. 增加持久化覆盖，验证非精确的作废会话不被恢复。
5. 运行定向测试、全仓测试和 lint；审查后合并部署开发环境。

