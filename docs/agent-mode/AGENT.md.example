# AGENT.md 示例（莫小派 Agent Mode V1.5）

本文件是 `AGENT.md` 内容编写指南 + 多场景示例。**实际生效的 AGENT.md 必须放在以下两个位置之一**（V1.5 D6 决策：只激活 2 级）：

| 级 | 实际路径 | 说明 |
|---|---|---|
| 1 | `/etc/numind/AGENT.md` | 部署级，运维写，全部用户生效 |
| 2 | `<user_data_dir>/users/<user_id>/AGENT.md` | 用户全局，每个 user_id 独立 |

**加载顺序**：低 → 高优先级（部署级在前，user-global 在后）。两个文件用 `\n\n---\n\n` 分隔后注入 system prompt 第 3 段（Memories）。

**约束**：
- 单文件 ≤ 12KB
- 总和 ≤ 50KB
- 超出会被截断，末尾追加 `[truncated: ...]` 标记
- 写中文或英文都行；中文友好

> V2 扩展（**当前未实施**，仅供未来参考）：路径 3-6 = workspace 级 `<workspace>/.numind/AGENT.md` / 项目根 `<workspace>/AGENT.md` / 规则目录 `.numind/rules/*.md` / local override `AGENT.local.md`。

---

## 部署级示例 — `/etc/numind/AGENT.md`

公司 / 部署级约定，运维统一写：

```markdown
# 莫小派部署级规则

## 通用约定
- 默认用简体中文回复（除非用户明确要求英文）
- 保持专业克制，不过度营销 / 不使用感叹号堆砌
- 敏感信息（电话、邮箱、身份证）出现时必须脱敏后再展示
- 涉及客户数据时永远不要 echo 完整数据到日志或公开输出

## 工具使用偏好
- 优先用知识库检索给答案，找不到再用 web_search 兜底
- bash_exec 工具仅用于明确的脚本化任务，不要随意调
```

---

## 用户全局示例 — `<user_data_dir>/users/<user_id>/AGENT.md`

用户本人的偏好 + 工作背景，**每个使用 agent 的角色**都可以根据自己的场景写：

### 场景 A — 销售员（医疗器械销售）

```markdown
# 我的偏好

- 我是医疗器械销售，专攻三甲医院骨科 / 心内科
- 客户开场白偏好"专业 + 简洁"，避免过度热情的话术
- 输出建议用客户能"现在就发出去"的完整话术，不要给可选模板让我自己拼

# 历史背景

- 上季度主推产品：XX 关节假体 / YY 心脏支架
- 主要客户群体：副主任医师及以上
```

### 场景 B — 制造业 SOP 操作员

```markdown
# 我的工作

- 我负责 A2 产线的注塑机日常质检 SOP
- 班次：早 8:00 - 晚 8:00，会反复跑同一组 SOP

# 偏好

- 报告生成时附带"异常历史对比"，不要每次只看当天数据
- 涉及停机决策时，先列影响面（产能损失估算）再给结论
- 不要展开冗长的设备原理说明 — 我熟悉设备，要的是操作指令
```

### 场景 C — 数据分析师

```markdown
# 我的工作

- 我是商业分析师，主要分析销售 / 营销数据
- 常用工具：SQL / Python pandas / Excel pivot table

# 偏好

- 数据分析结果偏好用结构化输出：摘要 → 关键指标表 → 异常项 → 建议
- 给 SQL 时直接给可执行 SQL，不要附"以下是示例"等套话
- 涉及 BI 报表口径时，先确认我用的是哪个 OLAP 库再回答
```

### 场景 D — PPT 制作 / 内容创作

```markdown
# 我的工作

- 我是市场部内容策划，每周需要做客户提案 PPT
- 关注：行业洞察 / 案例梳理 / 数据可视化建议

# 偏好

- 文案偏好"专业、轻快、有数据支撑"的风格，不要鸡汤体
- 配色和版式建议先给出整体设计语言（如 Bento / 极简 / 商务等），再展开细节
- 涉及客户 logo 时永远不要随便发挥设计 — 严格按客户 brand guideline
```

---

## 编写建议

1. **聚焦"个人化对待"** — 写"我是谁、我偏好什么"，而不是写通用知识（那是 SOP / 知识库的事）
2. **保持简洁** — 12KB 单文件上限，超出会被截断。一般 1-3KB 就够
3. **使用 markdown** — 标题 / bullet 列表 / 强调，LLM 都能识别
4. **避免敏感信息** — AGENT.md 会被注入到 LLM prompt，**不要写密码 / API key / 客户敏感数据**
5. **可随时迭代** — 用得不顺手就改，下一次 agent_run 会读到新版本

---

## V1.5 vs V2

V1.5 **只激活 2 级**，因为有数当前架构里没有清晰的"workspace"概念（不是 IDE，是 web SaaS）。等未来出现项目级需求（比如某个 `agent_definition_id` 绑定一组规则）再启用 3-6 层。

当前已预留扩展接口：`LoadAgentMd` 函数签名可以加可选参数 `WithWorkspace(dir string)`，`buildCandidates` 在 workspaceDir 非空时会追加后 4 个路径。**V1.5 spec 和代码均不实施**。
