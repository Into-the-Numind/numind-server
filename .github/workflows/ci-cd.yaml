name: Numind CI/CD

on:
  push:
    branches:
      - main
      - develop
      - release
      - product
      - '**'
  pull_request:
    branches:
      - main
      - develop
      - release
      - product

env:
  DOCKERHUB_USERNAME: neozhang96
  DOCKERHUB_PASSWORD: Docker1234
  IMAGE_NAME: neozhang96/numind-server
  SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
  DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
  DEV_HOST: ${{ secrets.DEV_HOST }}
  QA_HOST: ${{ secrets.QA_HOST }}
  PROD_HOST: ${{ secrets.PROD_HOST }}

jobs:
  # 代码构建
  code_build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.24.2
          cache: true

      - name: Install Task
        uses: arduino/setup-task@v1
        with:
          version: '3.x'

      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Force clean and reset
        run: |
          echo "=== Force cleaning all caches ==="
          go clean -modcache
          go clean -cache
          go clean -testcache
          rm -rf ~/.cache/go-build
          rm -rf ~/go/pkg/mod
          echo "=== Go environment ==="
          go env | grep -E "(GOOS|GOARCH|CGO|GOROOT|GOPATH)"

      - name: Get dependencies
        run: |
          go clean -modcache
          go clean -cache
          go mod download
          go mod tidy
          go mod verify

      - name: Build (compile check)
        run: go build ./...

      - name: Build application
        run: task build-prod

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: numind-binaries
          path: |
            ./bin/numind-linux-amd64
            ./bin/numind-darwin-amd64
            ./bin/numind-darwin-arm64
          retention-days: 30

      - name: Upload binary for deployment
        uses: actions/upload-artifact@v4
        with:
          name: numind-binary
          path: ./bin/numind-linux-amd64
          retention-days: 30

  # Docker 镜像构建 - 只在特定分支触发
  image_build:
    needs: code_build
    runs-on: ubuntu-latest
    timeout-minutes: 20
    if: github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/release' || github.ref == 'refs/heads/product'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0



      - name: Login to Docker Hub
        run: |
          # 检查Docker环境
          echo "检查Docker环境..."
          docker --version
          docker info
          # 登录到Docker Hub
          echo ${{ env.DOCKERHUB_PASSWORD }} | docker login -u ${{ env.DOCKERHUB_USERNAME }} --password-stdin



      - name: Build and push Docker image
        run: |
          # 显示构建信息
          echo "构建镜像: ${{ env.IMAGE_NAME }}:${{ github.ref_name }}"
          echo "当前分支: ${{ github.ref_name }}"
          # 构建镜像
          docker build -t ${{ env.IMAGE_NAME }}:${{ github.ref_name }} .
          # 推送镜像
          docker push ${{ env.IMAGE_NAME }}:${{ github.ref_name }}
          # 推送latest标签
          docker tag ${{ env.IMAGE_NAME }}:${{ github.ref_name }} ${{ env.IMAGE_NAME }}:latest
          docker push ${{ env.IMAGE_NAME }}:latest

  # 开发环境部署 - 只在 develop 分支触发
  deploy_dev:
    needs: image_build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.ref == 'refs/heads/develop'
    environment: development
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Development Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 49.233.219.254
          username: root
          password: Numind2025
          port: 22
          script: |
            # 设置环境变量
            export GITHUB_ACTOR="${{ github.actor }}"
            
            # 登录到Docker Hub
            echo "Docker1234" | docker login -u neozhang96 --password-stdin
            
            # 拉取最新镜像
            docker pull neozhang96/numind-server:develop

            # 清理 dangling (<none>) 镜像，避免磁盘膨胀
            echo "Pruning dangling images..."
            docker image prune -f || true
            
            # 检查并停止运行中的容器
            if docker ps -q -f name=numind-server-dev | grep -q .; then
              echo "Container numind-server-dev is running, stopping it..."
              docker stop numind-server-dev
            fi
            
            # 检查并删除存在的容器
            if docker ps -aq -f name=numind-server-dev | grep -q .; then
              echo "Container numind-server-dev exists, removing it..."
              docker rm numind-server-dev
            fi
            
            # 创建必要的目录并设置权限（确保容器内 UID 1001 可写）
            echo "Creating necessary directories..."
            sudo mkdir -p /opt/numind/dev/image/upload/{avatars,card,book}
            sudo chown -R 1001:1001 /opt/numind/dev || true
            sudo chmod -R 775 /opt/numind/dev
            
            # 启动新容器
            echo "Starting new container..."
            docker run -d \
              --name numind-server-dev \
              -p 9091:9091 \
              -p 9092:9092 \
              -v /opt/numind/dev:/opt/numind/dev \
              -v /etc/ssl/certimate/youshu.asia:/etc/ssl/certimate/youshu.asia:ro \
              --restart always \
              neozhang96/numind-server:develop \
              -c /app/config_dev.yaml
            
            # 等待容器启动
            echo "Waiting for container to start..."
            sleep 15
            
            # 检查容器状态
            if docker ps -q -f name=numind-server-dev | grep -q .; then
              echo "Container started successfully!"
              
                          # 等待容器启动并检查一次健康状态
            echo "Waiting for container to start and checking health..."
            sleep 10
            
            # 检查容器是否正在运行
            if docker ps -q -f name=numind-server-dev | grep -q .; then
              echo "Container is running, checking health endpoint..."
              
              # 尝试访问健康端点
              if curl -f http://localhost:9091/healthz > /dev/null 2>&1; then
                echo "Health check passed! Deployment completed successfully!"
                echo "Container status:"
                docker ps -f name=numind-server-dev --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
              else
                echo "Warning: Health endpoint not responding, but container is running"
                echo "Container logs:"
                docker logs --tail 10 numind-server-dev || true
                echo "Container status:"
                docker ps -f name=numind-server-dev --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
              fi
            else
              echo "Error: Container failed to start"
              docker logs numind-server-dev || true
              exit 1
            fi
            else
              echo "Error: Container failed to start"
              docker logs numind-server-dev || true
              exit 1
            fi

  # QA 环境部署 - 只在 release 分支触发
  deploy_qa:
    needs: image_build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.ref == 'refs/heads/release'
    environment: qa
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to QA Environment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 49.233.219.254
          username: root
          password: Numind2025
          script: |
            # 设置环境变量
            export GITHUB_ACTOR="${{ github.actor }}"
            
            # 登录到Docker Hub
            echo "Docker1234" | docker login -u neozhang96 --password-stdin
            
            # 拉取最新镜像
            docker pull neozhang96/numind-server:release

            # 清理 dangling (<none>) 镜像，避免磁盘膨胀
            echo "Pruning dangling images..."
            docker image prune -f || true
            
            # 检查并停止运行中的容器
            if docker ps -q -f name=numind-server-qa | grep -q .; then
              echo "Container numind-server-qa is running, stopping it..."
              docker stop numind-server-qa
            fi
            
            # 检查并删除存在的容器
            if docker ps -aq -f name=numind-server-qa | grep -q .; then
              echo "Container numind-server-qa exists, removing it..."
              docker rm numind-server-qa
            fi
            
            # 创建必要的目录并设置权限（确保容器内 UID 1001 可写）
            echo "Creating necessary directories..."
            sudo mkdir -p /opt/numind/qa/image/upload/{avatars,card,book}
            sudo chown -R 1001:1001 /opt/numind/qa || true
            sudo chmod -R 775 /opt/numind/qa
            
            # 启动新容器
            echo "Starting new container..."
            docker run -d \
              --name numind-server-qa \
              -p 9093:9091 \
              -p 9094:9092 \
              -v /opt/numind/qa:/opt/numind/qa \
              -v /etc/ssl/certimate/youshu.asia:/etc/ssl/certimate/youshu.asia:ro \
              --restart always \
              neozhang96/numind-server:release \
              -c /app/config_qa.yaml
            
            # 等待容器启动
            echo "Waiting for container to start..."
            sleep 15
            
            # 检查容器状态
            if docker ps -q -f name=numind-server-qa | grep -q .; then
              echo "Container started successfully!"
              
                          # 等待容器启动并检查一次健康状态
            echo "Waiting for container to start and checking health..."
            sleep 10
            
            # 检查容器是否正在运行
            if docker ps -q -f name=numind-server-qa | grep -q .; then
              echo "Container is running, checking health endpoint..."
              
              # 尝试访问健康端点
              if curl -f http://localhost:9093/healthz > /dev/null 2>&1; then
                echo "Health check passed! QA deployment completed successfully!"
                echo "Container status:"
                docker ps -f name=numind-server-qa --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
              else
                echo "Warning: Health endpoint not responding, but container is running"
                echo "Container logs:"
                docker logs --tail 10 numind-server-qa || true
                echo "Container status:"
                docker ps -f name=numind-server-qa --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
              fi
            else
              echo "Error: Container failed to start"
              docker logs numind-server-qa || true
              exit 1
            fi
            else
              echo "Error: Container failed to start"
              docker logs numind-server-qa || true
              exit 1
            fi

  # 生产环境部署 - 只在 product 分支触发
  deploy_product:
    needs: image_build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.ref == 'refs/heads/product'
    environment: production
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Production Environment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 49.233.219.254
          username: root
          password: Numind2025
          script: |
            # 设置环境变量
            export GITHUB_ACTOR="${{ github.actor }}"
            
            # 登录到Docker Hub
            echo "Docker1234" | docker login -u neozhang96 --password-stdin
            
            # 拉取最新镜像
            docker pull neozhang96/numind-server:product

            # 清理 dangling (<none>) 镜像，避免磁盘膨胀
            echo "Pruning dangling images..."
            docker image prune -f || true
            
            # 检查并停止运行中的容器
            if docker ps -q -f name=numind-server-prod | grep -q .; then
              echo "Container numind-server-prod is running, stopping it..."
              docker stop numind-server-prod
            fi
            
            # 检查并删除存在的容器
            if docker ps -aq -f name=numind-server-prod | grep -q .; then
              echo "Container numind-server-prod exists, removing it..."
              docker rm numind-server-prod
            fi
            
            # 创建必要的目录并设置权限（确保容器内 UID 1001 可写）
            echo "Creating necessary directories..."
            sudo mkdir -p /opt/numind/prod/image/upload/{avatars,card,book}
            sudo chown -R 1001:1001 /opt/numind/prod || true
            sudo chmod -R 775 /opt/numind/prod
            
            # 启动新容器
            echo "Starting new container..."
            docker run -d \
              --name numind-server-prod \
              -p 9095:9091 \
              -p 9096:9092 \
              -v /opt/numind/prod:/opt/numind/prod \
              -v /etc/ssl/certimate/youshu.asia:/etc/ssl/certimate/youshu.asia:ro \
              --restart always \
              neozhang96/numind-server:product \
              -c /app/config_prod.yaml
            
            # 等待容器启动
            echo "Waiting for container to start..."
            sleep 15
            
            # 检查容器状态
            if docker ps -q -f name=numind-server-prod | grep -q .; then
              echo "Container started successfully!"
              
              # 等待容器启动并检查一次健康状态
              echo "Waiting for container to start and checking health..."
              sleep 10
              
              # 检查容器是否正在运行
              if docker ps -q -f name=numind-server-prod | grep -q .; then
                echo "Container is running, checking health endpoint..."
                
                # 尝试访问健康端点
                if curl -f http://localhost:9095/healthz > /dev/null 2>&1; then
                  echo "Health check passed! Production deployment completed successfully!"
                  echo "Container status:"
                  docker ps -f name=numind-server-prod --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
                else
                  echo "Warning: Health endpoint not responding, but container is running"
                  echo "Container logs:"
                  docker logs --tail 10 numind-server-prod || true
                  echo "Container status:"
                  docker ps -f name=numind-server-prod --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
                fi
              else
                echo "Error: Container failed to start"
                docker logs numind-server-prod || true
                exit 1
              fi
            else
              echo "Error: Container failed to start"
              docker logs numind-server-prod || true
              exit 1
            fi
