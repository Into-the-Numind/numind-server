# Voiceprint microservice (CAM++ speaker embedding) — CPU only.
# Pin versions for reproducible builds. See DIARIZATION_SPEC §5 D1 / §7 T1.

onnxruntime==1.18.1
numpy==1.26.4
fastapi==0.111.1
uvicorn==0.30.3

# torch + torchaudio (CPU) are used ONLY for the Kaldi-compatible fbank front-end
# in compute_fbank() — NOT for model inference (that runs through onnxruntime).
# Future optimization: replace torchaudio.compliance.kaldi.fbank with
# kaldi-native-fbank to drop the heavy torch dependency from this service.
# CPU wheels are installed in the Dockerfile via the official PyTorch CPU index
# (--index-url .../whl/cpu) BEFORE this file, so these plain pins resolve as already
# satisfied. Do NOT use the Tsinghua mirror for torch (it lacks +cpu tags) nor an
# additive --extra-index-url (pip then picks the CUDA build → ~5.8GB image).
torch==2.2.2
torchaudio==2.2.2
