# =============================================================================
# Numind Sandbox Skill Image — Pinned Python Requirements (Layer 2)
# Track 4 / V1.5.0
#
# Version pinning strategy (T13 decision):
#   - Patch versions locked (==X.Y.Z) for reproducible builds.
#   - Update by running: pip-compile --upgrade requirements.in
#     or manually bumping patch versions after testing.
#
# All packages installed in Layer 2 (Dockerfile.libs).
# =============================================================================

# ─── Office document generation ─────────────────────────────────────────────
# Excel (xlsx-author skill, T4.5)
openpyxl==3.1.5

# Word / DOCX (docx-author skill, T4.6)
python-docx==1.1.2

# PowerPoint / PPTX (pptx-author skill, T4.7)
python-pptx==1.0.2

# ─── PDF generation ──────────────────────────────────────────────────────────
# HTML → PDF (pdf-from-html skill, T4.8)
# Requires: libpango, libcairo, libgdk-pixbuf (in Dockerfile.base / apt)
weasyprint==62.3

# Low-level PDF / fallback
reportlab==4.2.5

# ─── Data / chart generation ────────────────────────────────────────────────
# Tabular data manipulation
pandas==2.2.3

# Image processing and chart rendering
Pillow==10.4.0

# Matplotlib for 2D charts (used by xlsx-author chart embedding)
matplotlib==3.9.4

# ─── Utilities ────────────────────────────────────────────────────────────────
# HTTP requests (for fetching remote data in sandbox — disabled by --network=none
# in production; available for local integration testing)
requests==2.32.3

# JSON5 / TOML parsers (some skill configs)
tomli==2.2.1
