run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
  disable:
    - typecheck  # GORM 的某些用法会误报

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
